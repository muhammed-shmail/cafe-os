FROM node:20-alpine

WORKDIR /app

# Copy entire platform directory (has package.json and all source)
COPY platform/ .

# Install dependencies
RUN npm ci

# Generate Prisma client and build Next.js
RUN npm run db:generate && npm run build

EXPOSE 3000

CMD ["npm", "run", "-w", "@cafeos/web", "start"]
